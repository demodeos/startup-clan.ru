<form method="post" action="{{ path('app_ustav_delete', {'id': ustav.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ustav.id) }}">
    <button class="btn">Delete</button>
</form>
